import React from 'react'
import Balance from './Balance';

const Header = (props) => {
    return (
        <header>
            <Balance title="Balance" />
        </header>
    )
}

export default Header
